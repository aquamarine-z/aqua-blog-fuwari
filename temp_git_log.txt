commit c8ec720937b038e24c089130d351e48778de2fa0
Author: 24180 <2418013844@qq.com>
Date:   Mon Jun 15 10:56:45 2026 +0800

    feat: refactor category tree sorting and enable tree widget for blog

diff --git a/src/components/widget/Categories.astro b/src/components/widget/Categories.astro
index 283fc8d..addfb12 100644
--- a/src/components/widget/Categories.astro
+++ b/src/components/widget/Categories.astro
@@ -1,7 +1,7 @@
 ---
 import I18nKey from "../../i18n/i18nKey";
 import { i18n } from "../../i18n/translation";
-import { getBlogCategoryList, getDocsCategoryTree } from "../../utils/content-utils";
+import { getBlogCategoryList, getDocsCategoryTree, getBlogCategoryTree } from "../../utils/content-utils";
 import ButtonLink from "../control/ButtonLink.astro";
 import WidgetLayout from "./WidgetLayout.astro";
 import CategoryTree from "./CategoryTree.svelte";
@@ -15,12 +15,13 @@ const className = Astro.props.class;
 const style = Astro.props.style;
 const isDocs = Astro.props.isDocs;
 
-let categories = [];
-let categoryTree = [];
+let categories: any[] = [];
+let categoryTree: any[] = [];
 if (isDocs) {
-	categoryTree = await getDocsCategoryTree();
+	categoryTree = await getDocsCategoryTree(Astro.currentLocale);
 } else {
-	categories = await getBlogCategoryList();
+	categoryTree = await getBlogCategoryTree(Astro.currentLocale);
+	categories = await getBlogCategoryList(Astro.currentLocale);
 }
 
 const COLLAPSED_HEIGHT = "7.5rem";
@@ -36,15 +37,7 @@ const isCollapsed = !isDocs && categories.length >= COLLAPSE_THRESHOLD;
 	{isDocs ? (
 		<CategoryTree client:load categories={categoryTree} lang={Astro.currentLocale} currentUrl={Astro.url.pathname} />
 	) : (
-		categories.map((c) =>
-			<ButtonLink
-				url={c.url}
-				badge={String(c.count)}
-				label={`View all posts in the ${c.name.trim()} category`}
-			>
-				{c.name.trim()}
-			</ButtonLink>
-		)
+		<CategoryTree client:load categories={categoryTree} lang={Astro.currentLocale} currentUrl={Astro.url.pathname} />
 	)}
 </WidgetLayout>
 

commit 0af7c941fe0a2bec61750ec8fa8917a4630f2b68
Author: 24180 <2418013844@qq.com>
Date:   Sun Jun 14 13:47:45 2026 +0800

    feat:init

diff --git a/src/components/widget/Categories.astro b/src/components/widget/Categories.astro
index 8201649..283fc8d 100644
--- a/src/components/widget/Categories.astro
+++ b/src/components/widget/Categories.astro
@@ -1,7 +1,7 @@
 ---
 import I18nKey from "../../i18n/i18nKey";
 import { i18n } from "../../i18n/translation";
-import { getCategoryList, getCategoryTree } from "../../utils/content-utils";
+import { getBlogCategoryList, getDocsCategoryTree } from "../../utils/content-utils";
 import ButtonLink from "../control/ButtonLink.astro";
 import WidgetLayout from "./WidgetLayout.astro";
 import CategoryTree from "./CategoryTree.svelte";
@@ -18,9 +18,9 @@ const isDocs = Astro.props.isDocs;
 let categories = [];
 let categoryTree = [];
 if (isDocs) {
-	categoryTree = await getCategoryTree('docs');
+	categoryTree = await getDocsCategoryTree();
 } else {
-	categories = await getCategoryList('blog');
+	categories = await getBlogCategoryList();
 }
 
 const COLLAPSED_HEIGHT = "7.5rem";

commit e279fa0776d00422bb18f1f0141b7416d112ed6d
Author: 24180 <2418013844@qq.com>
Date:   Sat Jun 13 23:53:18 2026 +0800

    feat:init

diff --git a/src/components/widget/Categories.astro b/src/components/widget/Categories.astro
index b44e9da..8201649 100644
--- a/src/components/widget/Categories.astro
+++ b/src/components/widget/Categories.astro
@@ -1,35 +1,50 @@
 ---
 import I18nKey from "../../i18n/i18nKey";
 import { i18n } from "../../i18n/translation";
-import { getCategoryList } from "../../utils/content-utils";
+import { getCategoryList, getCategoryTree } from "../../utils/content-utils";
 import ButtonLink from "../control/ButtonLink.astro";
 import WidgetLayout from "./WidgetLayout.astro";
-
-const categories = await getCategoryList();
-
-const COLLAPSED_HEIGHT = "7.5rem";
-const COLLAPSE_THRESHOLD = 5;
-
-const isCollapsed = categories.length >= COLLAPSE_THRESHOLD;
+import CategoryTree from "./CategoryTree.svelte";
 
 interface Props {
 	class?: string;
 	style?: string;
+	isDocs?: boolean;
 }
 const className = Astro.props.class;
 const style = Astro.props.style;
+const isDocs = Astro.props.isDocs;
+
+let categories = [];
+let categoryTree = [];
+if (isDocs) {
+	categoryTree = await getCategoryTree('docs');
+} else {
+	categories = await getCategoryList('blog');
+}
+
+const COLLAPSED_HEIGHT = "7.5rem";
+const COLLAPSE_THRESHOLD = 5;
+
+// If it's the tree view, we don't collapse the widget
+const isCollapsed = !isDocs && categories.length >= COLLAPSE_THRESHOLD;
 ---
 
-<WidgetLayout name={i18n(I18nKey.categories)} id="categories" isCollapsed={isCollapsed} collapsedHeight={COLLAPSED_HEIGHT}
+<WidgetLayout name={i18n(I18nKey.categories, Astro.currentLocale)} id="categories" isCollapsed={isCollapsed} collapsedHeight={COLLAPSED_HEIGHT}
                 class={className} style={style}
 >
-    {categories.map((c) =>
-        <ButtonLink
-            url={c.url}
-            badge={String(c.count)}
-            label={`View all posts in the ${c.name.trim()} category`}
-        >
-            {c.name.trim()}
-        </ButtonLink>
-    )}
-</WidgetLayout>
\ No newline at end of file
+	{isDocs ? (
+		<CategoryTree client:load categories={categoryTree} lang={Astro.currentLocale} currentUrl={Astro.url.pathname} />
+	) : (
+		categories.map((c) =>
+			<ButtonLink
+				url={c.url}
+				badge={String(c.count)}
+				label={`View all posts in the ${c.name.trim()} category`}
+			>
+				{c.name.trim()}
+			</ButtonLink>
+		)
+	)}
+</WidgetLayout>
+

commit f3bddb447ecb3c67abb38210fca4f23cf5075db3
Author: Aquamarine <77386843+aquamarine-z@users.noreply.github.com>
Date:   Sat Jun 13 17:11:06 2026 +0800

    Initial commit

diff --git a/src/components/widget/Categories.astro b/src/components/widget/Categories.astro
new file mode 100644
index 0000000..b44e9da
--- /dev/null
+++ b/src/components/widget/Categories.astro
@@ -0,0 +1,35 @@
+---
+import I18nKey from "../../i18n/i18nKey";
+import { i18n } from "../../i18n/translation";
+import { getCategoryList } from "../../utils/content-utils";
+import ButtonLink from "../control/ButtonLink.astro";
+import WidgetLayout from "./WidgetLayout.astro";
+
+const categories = await getCategoryList();
+
+const COLLAPSED_HEIGHT = "7.5rem";
+const COLLAPSE_THRESHOLD = 5;
+
+const isCollapsed = categories.length >= COLLAPSE_THRESHOLD;
+
+interface Props {
+	class?: string;
+	style?: string;
+}
+const className = Astro.props.class;
+const style = Astro.props.style;
+---
+
+<WidgetLayout name={i18n(I18nKey.categories)} id="categories" isCollapsed={isCollapsed} collapsedHeight={COLLAPSED_HEIGHT}
+                class={className} style={style}
+>
+    {categories.map((c) =>
+        <ButtonLink
+            url={c.url}
+            badge={String(c.count)}
+            label={`View all posts in the ${c.name.trim()} category`}
+        >
+            {c.name.trim()}
+        </ButtonLink>
+    )}
+</WidgetLayout>
\ No newline at end of file
