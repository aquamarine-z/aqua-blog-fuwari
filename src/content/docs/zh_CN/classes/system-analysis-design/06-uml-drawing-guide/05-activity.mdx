---
title: "五、 活动图（Activity Diagrams）"
published: 2026-06-26
description: "活动图与高级流程图的区别、核心元素含义说明（起点、终点、动作状态、控制流、决策/合并点）以及新闻抓取算法控制流活动图实例解析。"
sidebar_position: 5
---
import Mermaid from "@/components/Mermaid.astro";

活动图用于描述系统的工作流或算法逻辑流程，侧重于动作之间的控制流，类似于高级流程图。

### 1. 活动图核心元素含义说明

*   **起点与终点**：与状态图相同，用黑色实心圆点代表流程起点，双圆环代表流程终点。
*   **动作状态（Action / Activity）**：用**圆角矩形**表示，代表流程中需要执行的具体操作或步骤（如“发起HTTP请求”）。
*   **控制流（Control Flow）**：**带箭头的实线**，用于指出动作执行的前后时序关系。
*   **决策点与合并点（Decision / Merge）**：用**空心菱形**表示。
    *   **决策分流**：一条输入流进入，根据不同的**判定条件（用中括号 `[条件]` 标明守卫）**流出多条不同分支。
    *   **合并合流**：多条分支汇合进入，引出一条单一输出流向下传递。

### 2. 实战例图：自动柜员机（ATM）取款工作流

*   **题目背景**：某银行自动柜员机（ATM）系统中，客户插入银行卡后，系统校验卡片。若卡片无效则退卡终止流程；若有效则提示输入密码。系统校验密码，若错误则提示并退卡终止流程；若正确则提示输入取款金额。系统校验账户余额是否充足，若不足则提示并退卡终止流程；若充足则扣款、出钞，最后退卡并结束流程。请绘制该工作流的活动图。

<Mermaid code={`
flowchart TD
    Start([开始]) --> InsertCard[插入银行卡]
    InsertCard --> VerifyCard[校验卡片有效性]
    VerifyCard --> IsCardValid{卡片是否有效?}
    
    IsCardValid -- "[否]" --> EjectCard1[退卡] --> Terminate1([终止])
    IsCardValid -- "[是]" --> PromptPIN[提示输入密码]
    
    PromptPIN --> InputPIN[用户输入密码]
    InputPIN --> VerifyPIN[系统校验密码]
    VerifyPIN --> IsPINCorrect{密码是否正确?}
    
    IsPINCorrect -- "[否]" --> ShowPINError[显示密码错误提示] --> EjectCard2[退卡] --> Terminate2([终止])
    IsPINCorrect -- "[是]" --> InputAmount[提示并输入取款金额]
    
    InputAmount --> CheckBalance[系统校验账户余额]
    CheckBalance --> IsBalanceSufficient{余额是否充足?}
    
    IsBalanceSufficient -- "[否]" --> ShowBalanceError[提示余额不足] --> EjectCard3[退卡] --> Terminate3([终止])
    IsBalanceSufficient -- "[是]" --> DeductBalance[扣除账户余额]
    
    DeductBalance --> DispenseCash[出钞口出钞]
    DispenseCash --> EjectCard4[退还卡片] --> End([结束])

    classDef startEnd fill:#f8f9fa,stroke:#7f8c8d,stroke-width:1px,color:#2c3e50
    classDef normal fill:#f8f9fa,stroke:#dddddd,stroke-width:1px,color:#2c3e50
    classDef critical fill:#fdf2e9,stroke:#f39c12,stroke-width:3px,color:#d35400
    
    class Start,End,Terminate1,Terminate2,Terminate3 startEnd
    class InsertCard,VerifyCard,PromptPIN,InputPIN,VerifyPIN,InputAmount,CheckBalance,DeductBalance,DispenseCash normal
    class EjectCard1,EjectCard2,EjectCard3,EjectCard4,ShowPINError,ShowBalanceError critical
`} />
