---
import { getLangPaths } from '@utils/i18n-utils';
import { i18n } from "@i18n/translation";
import I18nKey from "@i18n/i18nKey";
export function getStaticPaths() {
    return getLangPaths();
}

import MainGridLayout from "../../layouts/MainGridLayout.astro";
import { siteConfig, profileConfig } from "../../config";
import { Icon } from "astro-icon/components";
import ImageWrapper from "../../components/misc/ImageWrapper.astro";
import { url } from "../../utils/url-utils";

const lang = Astro.currentLocale || siteConfig.lang;

import { HomeKey } from "../../i18n/partials/home/keys";
const t = (key: HomeKey) => i18n(key, lang);
---

<MainGridLayout title={i18n(I18nKey.home, Astro.currentLocale)}>
    <div class="flex w-full rounded-[var(--radius)] overflow-hidden relative mb-6">
        <div class="card-base z-10 px-6 py-10 md:px-12 md:py-16 relative w-full flex flex-col md:flex-row gap-8 md:gap-12 items-center md:items-start transition-all">
            <div class="w-36 h-36 md:w-52 md:h-52 shrink-0 rounded-full overflow-hidden border-[6px] border-[var(--primary)]/20 shadow-xl shadow-[var(--primary)]/10 hover:scale-105 hover:border-[var(--primary)]/40 transition-all duration-300">
                <ImageWrapper src={profileConfig.avatar || '/favicon.svg'} alt="Avatar" class="w-full h-full object-cover" />
            </div>
            
            <div class="flex-1 flex flex-col items-center md:items-start gap-5 text-center md:text-left mt-2">
                <h1 class="text-3xl md:text-5xl title-font font-bold text-[var(--primary)] tracking-tight">
                    {t(HomeKey.title)}
                </h1>
                
                <h2 class="text-xl md:text-2xl font-semibold text-90 h-8 flex items-center">
                    <span id="typewriter" class="border-r-[3px] border-[var(--primary)] pr-1 animate-pulse"></span>
                </h2>
                
                <p class="text-base md:text-lg text-75 leading-relaxed max-w-2xl" set:html={t(HomeKey.description)}>
                </p>
                
                <div class="flex flex-wrap justify-center md:justify-start gap-3 mt-2">
                    <div class="flex items-center gap-2 px-4 py-2 rounded-xl bg-[var(--primary)]/10 text-[var(--primary)] hover:bg-[var(--primary)]/20 hover:-translate-y-0.5 transition-all font-medium cursor-default">
                        <Icon name="fa6-brands:react" class="text-xl" /> React
                    </div>
                    <div class="flex items-center gap-2 px-4 py-2 rounded-xl bg-[var(--primary)]/10 text-[var(--primary)] hover:bg-[var(--primary)]/20 hover:-translate-y-0.5 transition-all font-medium cursor-default">
                        <Icon name="fa6-brands:vuejs" class="text-xl" /> Vue
                    </div>
                    <div class="flex items-center gap-2 px-4 py-2 rounded-xl bg-[var(--primary)]/10 text-[var(--primary)] hover:bg-[var(--primary)]/20 hover:-translate-y-0.5 transition-all font-medium cursor-default">
                        <Icon name="material-symbols:palette-outline" class="text-xl" /> {t(HomeKey.design)}
                    </div>
                    <div class="flex items-center gap-2 px-4 py-2 rounded-xl bg-[var(--primary)]/10 text-[var(--primary)] hover:bg-[var(--primary)]/20 hover:-translate-y-0.5 transition-all font-medium cursor-default">
                        <Icon name="material-symbols:translate-rounded" class="text-xl" /> JLPT N1 & TOPIK 6
                    </div>
                </div>

                <div class="flex flex-wrap gap-4 mt-6 w-full justify-center md:justify-start">
                    <a href={url(Astro.currentLocale && Astro.currentLocale !== siteConfig.lang ? `/${Astro.currentLocale}/blog/` : "/blog/")} class="group bg-[var(--primary)] text-white px-6 py-3.5 rounded-xl hover:brightness-110 shadow-lg shadow-[var(--primary)]/30 active:scale-95 transition-all font-bold flex items-center gap-2 text-lg">
                        <Icon name="material-symbols:article-outline-rounded" class="text-xl" />
                        {t(HomeKey.readBlog)}
                        <Icon name="material-symbols:arrow-forward-rounded" class="text-xl transition-transform group-hover:translate-x-1" />
                    </a>
                    <a href={url(Astro.currentLocale && Astro.currentLocale !== siteConfig.lang ? `/${Astro.currentLocale}/docs/intro/` : "/docs/intro/")} class="group bg-[var(--btn-regular-bg)] text-[var(--btn-content)] px-6 py-3.5 rounded-xl hover:bg-[var(--btn-regular-bg-hover)] shadow-md active:scale-95 transition-all font-bold flex items-center gap-2 text-lg">
                        <Icon name="material-symbols:menu-book-outline-rounded" class="text-xl" />
                        {t(HomeKey.viewDocs)}
                        <Icon name="material-symbols:arrow-forward-rounded" class="text-xl transition-transform group-hover:translate-x-1" />
                    </a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="card-base p-8 hover:shadow-lg hover:-translate-y-1 transition-all duration-300 group">
            <h3 class="text-2xl font-bold mb-5 flex items-center gap-3 text-[var(--primary)]">
                <div class="p-2 rounded-lg bg-[var(--primary)]/10 group-hover:bg-[var(--primary)]/20 transition-colors">
                    <Icon name="material-symbols:code-blocks-outline-rounded" class="text-2xl" />
                </div>
                {t(HomeKey.techStack)}
            </h3>
            <p class="text-75 mb-5 leading-relaxed">
                {t(HomeKey.techDesc)}
            </p>
            <ul class="space-y-2 text-75">
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> React, Vue.js, Svelte</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> Tailwind CSS, SCSS</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> TypeScript, Node.js</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> Astro, Next.js, Nuxt</li>
            </ul>
        </div>
        
        <div class="card-base p-8 hover:shadow-lg hover:-translate-y-1 transition-all duration-300 group">
            <h3 class="text-2xl font-bold mb-5 flex items-center gap-3 text-[var(--primary)]">
                <div class="p-2 rounded-lg bg-[var(--primary)]/10 group-hover:bg-[var(--primary)]/20 transition-colors">
                    <Icon name="material-symbols:translate-rounded" class="text-2xl" />
                </div>
                {t(HomeKey.languages)}
            </h3>
            <p class="text-75 mb-5 leading-relaxed">
                {t(HomeKey.langDesc)}
            </p>
            <ul class="space-y-2 text-75">
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> {t(HomeKey.native)}</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> {t(HomeKey.english)}</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> {t(HomeKey.japanese)}</li>
                <li class="flex items-center gap-3"><Icon name="material-symbols:check-circle-outline-rounded" class="text-[var(--primary)]" /> {t(HomeKey.korean)}</li>
            </ul>
        </div>
    </div>
</MainGridLayout>

<style>
@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@700;800&display=swap');

.title-font {
    font-family: 'Outfit', sans-serif;
}
</style>

<script is:inline define:vars={{ initialSubtitle: t(HomeKey.subtitle) }}>
    function initTypewriter() {
        const el = document.getElementById("typewriter");
        if (!el) return;
        
        const allWords = [
            initialSubtitle,
            "フロントエンドエンジニア · 多言語話者",
            "프론트엔드 개발자 · 다국어 구사자",
            "Frontend Engineer · Polyglot",
            "前端开发工程师 · 多语种学习者"
        ];
        // Remove duplicates so we don't type the same thing twice in a row
        const words = [allWords[0], ...allWords.slice(1).filter(w => w !== allWords[0])];
        
        let i = 0;
        let j = 0;
        let isDeleting = false;
        
        function type() {
            if (!document.getElementById("typewriter")) return; // element detached
            
            const currentWord = words[i];
            
            if (isDeleting) {
                j--;
            } else {
                j++;
            }
            
            el.textContent = currentWord.substring(0, j);
            
            let typeSpeed = 100;
            if (isDeleting) typeSpeed /= 2;
            
            if (!isDeleting && j === currentWord.length) {
                typeSpeed = 2000;
                isDeleting = true;
            } else if (isDeleting && j === 0) {
                isDeleting = false;
                i = (i + 1) % words.length;
                typeSpeed = 500;
            }
            
            setTimeout(type, typeSpeed);
        }
        
        type();
    }
    
    // Support Astro View Transitions
    document.addEventListener('astro:page-load', initTypewriter);
    if (document.readyState !== 'loading') {
        initTypewriter();
    } else {
        document.addEventListener('DOMContentLoaded', initTypewriter);
    }
</script>
